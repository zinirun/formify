import { Input } from 'antd';

export default function QTextType() {
    return (
        <div>
            <Input disabled={true} value="텍스트 타입" style={{ cursor: 'default' }} />
        </div>
    );
}
